{
  "name": "meeting-ai-anna",
  "version": "0.2.3",
  "private": true,
  "type": "module",
  "description": "Meeting AI on Anna — detect questions, answer them, and recap meetings. Two live surfaces: an Anna App (anna.llm.complete) and an Executa plugin (host sampling). No API key.",
  "engines": { "node": ">=18" },
  "scripts": {
    "dev": "anna-app dev",
    "validate": "anna-app validate",
    "executa:dev": "cd executas/meeting-ai && anna-app executa dev",
    "executa:test": "node executas/meeting-ai/test-roundtrip.mjs"
  }
}
